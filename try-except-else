# coding=utf-8
#时间：2017.4.13
#地点：lab211
#如果执行了try，则except被忽略，但是else被执行。
#如果执行了except，在这段代码中，会一直被要求重新输入。
while 1:
    a=raw_input('the first number:')
    b=raw_input('the second number:')
    try:
        c=float(a)/float(b)
    except Exception,e:
        print e
        print 'try again'
    else:
        break
